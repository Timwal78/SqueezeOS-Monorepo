from setuptools import setup, find_packages

setup(
    name="proof402",
    version="1.0.0",
    description="402Proof FastAPI/Flask middleware — XRP/RLUSD agent payment verification",
    packages=find_packages(),
    python_requires=">=3.9",
    keywords=["x402", "xrpl", "rlusd", "payment", "ai-agents"],
)
